{% extends "admin/base.html.twig" %}

{% block body %}
    <div class="content-wrapper">
    <section class="content-header">
        <div class="container-fluid">
            <div class="row mb-2">
                <div class="col-sm-6">
                    <h1>網站基本設定</h1>
                </div>
                <div class="col-sm-6">
                    <ol class="breadcrumb float-sm-right">
                        <li class="breadcrumb-item"><a href="{{ url("admin.index") }}">Home Page</a></li>
                        <li class="breadcrumb-item active">網站基本設定</li>
                    </ol>
                </div>
            </div>
        </div>
    </section>
    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-6 mb-4">
                <!-- Approach -->
                <div class="card shadow mb-4">
                    <div class="card-header py-3">
                        <h6 class="m-0 font-weight-bold text-primary">網站基本設定</h6>
                    </div>
                    <div class="card-body">
                        {{ form_start(form) }}
                        <div class="form-group">
                            {{ form_label(form.title) }}
                            {{ form_widget(form.title, {'attr': {'class': 'form-control'}}) }}
                        </div>
                        <div class="form-group">
                            {{ form_label(form.description) }}
                            {{ form_widget(form.description, {'attr': {'class': 'form-control'}}) }}
                        </div>
                        <div class="form-group">
                            {{ form_label(form.keyword) }}
                            {{ form_widget(form.keyword, {'attr': {'class': 'form-control'}}) }}
                        </div>
{#                        <div class="form-group">#}
{#                            {{ form_label(form.shippingStandard) }}#}
{#                            {{ form_widget(form.shippingStandard, {'attr': {'class': 'form-control'}}) }}#}
{#                        </div>#}
                        <button class="btn btn-primary" type="submit">儲存設定</button>
                        {{ form_end(form) }}
                    </div>
                </div>

            </div>
        </div>
    </div>
    </div>

{% endblock %}