<!-- Navbar -->
<nav class="main-header navbar navbar-expand navbar-white navbar-light">
    <!-- Left navbar links -->
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link" data-widget="pushmenu" href="#"><i class="fas fa-bars"></i></a>
        </li>
    </ul>

    <!-- Right navbar links -->
    <ul class="navbar-nav ml-auto">
        <!-- Notifications Dropdown Menu -->
        {#<li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle"
               data-toggle="dropdown"
               href="#"
               role="button"
               aria-haspopup="true"
               aria-expanded="false">
                <i class="fas fa-language"></i> &nbsp;&nbsp;&nbsp; {{ "Switch Language" | trans }}
            </a>
            <div class="dropdown-menu dropdown-menu-right">
                <a class="dropdown-item" href="{{ url('switch_locale', {'locale': 'zh_tw'}) }}">
                    {{ "Traditional Chinese" | trans }}
                </a>
                <a class="dropdown-item" href="{{ url('switch_locale', {'locale': 'zh_cn'}) }}">
                    {{ "Simplified Chinese" | trans }}
                </a>
                <a class="dropdown-item" href="{{ url('switch_locale', {'locale': 'en'}) }}">
                    {{ "English" | trans }}
                </a>
                <a class="dropdown-item" href="{{ url('switch_locale', {'locale': 'jp'}) }}">
                    {{ "Japanese" | trans }}
                </a>
            </div>
        </li>#}
        <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle"
               data-toggle="dropdown"
               href="#"
               role="button"
               aria-haspopup="true"
               aria-expanded="false">
                <i class="fas fa-user-circle"></i> &nbsp;&nbsp;&nbsp; {{ "Welcome" | trans }}
            </a>
            <div class="dropdown-menu dropdown-menu-right">
                <!--<a class="dropdown-item" href="#">個人資料設定</a>
                <div class="dropdown-divider"></div>-->
                <a class="dropdown-item" href="{{logout_path("main") }}">{{ "Logout" | trans }}</a>
            </div>
        </li>
    </ul>
</nav>
<!-- /.navbar -->